---
/**
 * Overrides Starlight's dark/light/auto picker with nothing.
 *
 * The rest of the site is dark-only — the marketing pages have no light theme
 * and the tokens in global.css are dark values — so the picker offered choices
 * that didn't work. See ThemeProvider.astro, which pins the theme to dark.
 */
---
